<% if @error.present? %>
  showError('<%= j @error %>');
<% else %>
  banMembership(<%= j @service.membership.id.to_s %>);
  showSuccess('<%= j t('admin2.manage_users.saved') %>');
  processDeleteUser('<%= @service.membership.id %>', <%= @can_delete %>, '<%= j @delete_title %>');
<% end %>

<% if @error.present? %>
  showError('<%= j @error %>');
<% else %>
  <% if @allow_id %>
    allowPost(<%= j @id %>);
  <% end %>
  <% if @disallow_id %>
    disallowPost(<%= j @id %>);
  <% end %>
  showSuccess('<%= j t('admin2.manage_users.saved') %>');
<% end %>
